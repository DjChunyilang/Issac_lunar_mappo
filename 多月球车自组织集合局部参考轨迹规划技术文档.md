# 多月球车自组织集合局部参考轨迹规划技术文档
## 代理训练与 Isaac/PhysX 高保真评估路线

> 本文只描述多月球车自组织集合任务的技术方案。当前实现口径已经从原 V2.0 的“Isaac Sim / Isaac Lab 作为主要训练平台”修订为“高吞吐 proxy 训练 + Isaac Sim / Isaac Lab / PhysX 高保真闭环评估”。工程目录、实施排期、运行命令和当前实验状态分别见 `docs/scaffold.md`、`docs/implementation_plan.md`、`docs/runbooks/` 和 `docs/current_status.md`。

---

## 1. 文档范围

本文档用于定义多月球车自组织集合任务的技术方案，包括任务建模、信息边界、强化学习建模方式、观测空间、critic 状态空间、动作空间、轨迹生成流程、简化控制链路、奖励函数、网络结构、训练闭环和评估判据。

当前训练主路径采用 PyTorch / torch-vectorized proxy 环境和 SKRL-MAPPO。Isaac Sim / Isaac Lab / PhysX 当前不参与每次 PPO/MAPPO 采样更新，而用于 checkpoint 级高保真闭环评估、迁移 sanity check、失效分析和可视化展示。Actor 不直接输出高维轨迹点序列，而输出低维局部子目标动作；局部参考轨迹由确定性轨迹生成器生成，proxy 训练中由简化速度控制器和 2D/2.5D 运动学模型推进状态，高保真评估中再通过 PhysX 轮式资产控制适配器验证轨迹跟踪与姿态稳定性。

---

## 2. 任务定义

设系统包含 $N=4$ 个同构月球车。第 $i$ 个智能体在时刻 $t$ 的状态记为 $x_i(t)$，位置为 $p_i(t)\in\mathbb{R}^3$，速度为 $v_i(t)$，航向角为 $\psi_i(t)$。

任务目标是在月面 2.5D/3D 地形条件下，使多个智能体通过局部感知与邻域通信完成自组织集合。执行阶段不向智能体显式提供集合点。智能体仅依据自身状态、邻居状态共享信息、局部地形特征和局部聚集态势进行分散决策。

训练阶段允许引入外部算法给出的最优集合点 $p^{*}(t)$。该点仅用于 centralized critic、oracle 辅助奖励和评估指标计算，不作为 actor 的观测输入。该设计保持集中式训练、分散式执行的信息边界。

当前阶段不考虑机械拼接与精细对接，仅以几何聚集成功作为任务完成标准。

---

## 3. 当前总体技术路线

主技术路线定义为：

$$
\text{Proxy Vectorized Training}
+
\text{SKRL-MAPPO}
+
\text{低维局部子目标动作}
+
\text{确定性轨迹生成器}
+
\text{简化速度跟踪控制器}
+
\text{Isaac/PhysX Checkpoint Evaluation}.
$$

与原 V2.0 表述相比，关键变更如下：

| 维度 | 原 V2.0 表述 | 当前技术路线 |
|---|---|---|
| 训练主路径 | Isaac Lab / PhysX 多车物理训练 | torch-vectorized proxy 环境 |
| 高保真仿真 | 训练 loop 的物理推进层 | checkpoint 级闭环评估层 |
| 动作接口 | 低维局部子目标 $[\rho,\beta]$ | 保持不变 |
| 轨迹与控制 | 子目标 -> 轨迹 -> 简化控制 | proxy 中用于运动学推进，PhysX 中通过轮式资产适配器验证 |
| 结果声明 | Isaac 物理训练成功 | proxy strict pass + PhysX closed-loop evaluation |

系统分为五个技术层。

第一层为 **proxy 多智能体任务层**。该层负责多环境并行采样、reset / step、观测构造、动作解释、奖励计算、终止判定、oracle 辅助信息计算和 metrics 记录。当前 proxy 动力学仍是 2D/2.5D torch-vectorized 运动学模型，可配置为兼容旧实验的 kinematic unicycle 或新工程探针使用的 kinematic bicycle；地形开启时查询 procedural heightfield / crater proxy 特征并施加速度缩放，但不包含质量、惯量、轮地接触、打滑、沉陷、悬挂或 PhysX contact。

第二层为 **SKRL-MAPPO 训练层**。该层实现 centralized training with decentralized execution。Actor 执行期仅接收局部观测，critic 训练期接收全局状态和 oracle 辅助信息。`isaaclab-multi-agent` wrapper 只是 SKRL 接口适配层，不代表训练 loop 运行在 Isaac Sim / PhysX 中。

第三层为 **动作解释与轨迹生成层**。该层将 actor 输出的低维局部子目标动作 $[\rho_i,\beta_i]$ 转换为局部参考轨迹 $\mathcal{T}_i(t)$。

第四层为 **简化控制与 proxy 状态推进层**。该层将局部参考轨迹转换为车体速度命令 $[v_i^{\text{cmd}},\omega_i^{\text{cmd}}]$，再通过运动学积分更新 rover proxy state。

第五层为 **Isaac/PhysX 高保真评估层**。该层低频接收候选 checkpoint 或参考轨迹，在 Isaac Sim / Isaac Lab / PhysX 场景中执行闭环轨迹跟踪，报告 tracking error、path completion、tilt、碰撞和失败案例。当前使用 Jackal 作为轮式资产 placeholder；它可验证控制链路和评估流程，但不能等同于最终月球车低重力轮壤动力学。

---

## 4. 符号约定

| 符号 | 含义 |
|---|---|
| $N$ | 智能体数量，当前固定为 4 |
| $i,j$ | 智能体索引 |
| $t$ | 离散规划时刻 |
| $\Delta t_p$ | 规划刷新周期 |
| $\Delta t_c$ | 控制执行周期 |
| $x_i(t)$ | 第 $i$ 个智能体全状态 |
| $p_i(t)\in\mathbb{R}^3$ | 第 $i$ 个智能体位置 |
| $v_i(t)$ | 第 $i$ 个智能体速度 |
| $\psi_i(t)$ | 第 $i$ 个智能体航向角 |
| $o_i(t)$ | 第 $i$ 个智能体局部观测 |
| $s(t)$ | centralized critic 输入状态 |
| $a_i(t)$ | 第 $i$ 个智能体动作 |
| $p^{*}(t)$ | 外部算法给出的最优集合点 |
| $d_i^{*}(t)$ | 第 $i$ 个智能体到 $p^{*}(t)$ 的距离 |
| $\bar d^{*}(t)$ | 平均最优点距离 |
| $\bar p(t)$ | 团队几何中心 |
| $D_{\max}(t)$ | 团队最大 pairwise 距离 |
| $\sigma_p^2(t)$ | 团队分散度 |
| $\mathcal{T}_i(t)$ | 第 $i$ 个智能体的局部参考轨迹 |
| $K$ | 轨迹生成器输出的轨迹点数量 |

其中：

$$
\bar p(t)=\frac{1}{N}\sum_{i=1}^{N}p_i(t),
$$

$$
D_{\max}(t)=\max_{i,j}\|p_i(t)-p_j(t)\|,
$$

$$
\sigma_p^2(t)=\frac{1}{N}\sum_{i=1}^{N}\|p_i(t)-\bar p(t)\|^2,
$$

$$
d_i^{*}(t)=\|p_i(t)-p^{*}(t)\|,
$$

$$
\bar d^{*}(t)=\frac{1}{N}\sum_{i=1}^{N}d_i^{*}(t).
$$

---

## 5. 系统运行闭环

训练闭环定义为：

$$
\text{proxy state}
\rightarrow
\text{actor observation / critic state}
\rightarrow
\text{SKRL-MAPPO actor 输出低维动作}
\rightarrow
\text{动作解释器生成局部子目标}
\rightarrow
\text{轨迹生成器生成局部参考轨迹}
\rightarrow
\text{简化速度跟踪控制器生成控制命令}
\rightarrow
\text{proxy 运动学状态更新}
\rightarrow
\text{奖励、终止与下一观测计算}.
$$

在每个规划步中，proxy 环境读取所有 rover 的状态、地形特征、邻居关系和安全事件，构造 actor 局部观测 $o_i(t)$ 与 critic 全局状态 $s(t)$。Actor 输出低维局部子目标动作后，动作解释器将其转换为局部参考轨迹，并通过简化控制器转换为速度命令。Proxy core 使用运动学积分推进状态，再计算奖励、终止标志和下一时刻观测。

高保真评估闭环定义为：

$$
\text{候选 checkpoint / 参考轨迹}
\rightarrow
\text{Isaac/PhysX 轮式资产控制适配}
\rightarrow
\text{PhysX 场景闭环推进}
\rightarrow
\text{tracking error / completion / tilt / failure metrics}.
$$

该闭环不参与每次 MAPPO 梯度更新，只用于 checkpoint 级验证和迁移风险诊断。

---

## 6. 信息边界

### 6.1 执行期信息

执行阶段，第 $i$ 个智能体可访问：

$$
o_i(t)=
\left[
o_i^{\text{ego}}(t),
o_i^{\text{nbr}}(t),
o_i^{\text{ter}}(t),
o_i^{\text{agg}}(t)
\right].
$$

其中，$o_i^{\text{ego}}(t)$ 为自车状态，$o_i^{\text{nbr}}(t)$ 为邻居状态共享信息，$o_i^{\text{ter}}(t)$ 为局部地形手工特征，$o_i^{\text{agg}}(t)$ 为局部聚集态势特征。

执行期 actor 不可访问 $p^{*}(t)$、$d_i^{*}(t)$、$\bar d^{*}(t)$ 等 oracle 信息。

### 6.2 训练期信息

训练阶段 centralized critic 可访问：

$$
s(t)=
\left[
s^{\text{agent}}(t),
s^{\text{team}}(t),
s^{\text{ter}}(t),
s^{\text{oracle}}(t)
\right].
$$

其中，$s^{\text{agent}}(t)$ 为全部智能体真值状态，$s^{\text{team}}(t)$ 为团队几何统计量，$s^{\text{ter}}(t)$ 为地形摘要，$s^{\text{oracle}}(t)$ 为外部最优集合点及其距离统计量。

### 6.3 通信机制

当前阶段通信机制采用邻居状态共享。设第 $i$ 个智能体的通信邻居集合为 $\mathcal{N}_i(t)$，则其可接收 $\mathcal{N}_i(t)$ 内邻居的状态摘要。

---

## 7. 观测空间

### 7.1 总体结构

actor 观测空间 schema 版本为 `ego_v3_local_terrain_grid`。多智能体观测张量形状为 $(N_{\text{env}}, N, d_o)$，其中 $N_{\text{env}}$ 为并行环境数，$N=4$ 为智能体数，$d_o=86$ 为每智能体观测维度。

actor 观测向量由四类特征沿最后一维拼接，拼接顺序固定：

$$
o_i(t)=
\left[
o_i^{\text{ego}}(t),\;
o_i^{\text{nbr}}(t),\;
o_i^{\text{ter}}(t),\;
o_i^{\text{agg}}(t)
\right]
\in \mathbb{R}^{86}.
$$

维度构成：

$$
d_o = d_{\text{ego}} + N_{\text{nbr}} \cdot d_{\text{nbr}} + d_{\text{ter}} + d_{\text{agg}}
= 10 + 3 \times 7 + 50 + 5 = 86.
$$

| 分量 | 维度 | 含义 |
|---|---:|---|
| $o_i^{\text{ego}}$ | 10 | 自车本体感知状态 |
| $o_i^{\text{nbr}}$ | $3\times 7=21$ | 通信半径内最近 3 个邻居的相对状态 |
| $o_i^{\text{ter}}$ | 50 | 车体系固定 $5\times5$ 局部地形网格 |
| $o_i^{\text{agg}}$ | 5 | 通信半径内可见邻居的局部聚合统计 |

其中仅 `communication_radius`（默认 6.0 m）可通过实验 YAML 覆盖。取值 $R_{\text{comm}}>0$ 时表示有限通信半径；取值 `<=0` 时表示临时取消通信距离限制，所有非自身 rover 均作为可见邻居。`max_neighbors`、各分量维度等参数硬编码于 `ObservationCfg`，不开放外部配置。

---

### 7.2 自车状态 $o_i^{\text{ego}}$（10 维）

自车状态描述第 $i$ 个智能体在世界坐标系中的运动学状态，是策略进行本体运动决策的基础。

| 维度索引 | 记号 | 含义 | 物理意义 |
|:---:|---|---|---|
| 0 | $x_i$ | 世界系 $x$ 坐标 | 水平面绝对位置，导航与目标判断的基准 |
| 1 | $y_i$ | 世界系 $y$ 坐标 | 同上 |
| 2 | $z_i$ | 世界系 $z$ 坐标（高度） | 车身当前高度，感知所处地形高程 |
| 3 | $\cos\psi_i$ | 航向角余弦 | 避免 $\pm\pi$ 处的不连续性，表示"朝哪个方向" |
| 4 | $\sin\psi_i$ | 航向角正弦 | 与余弦配对构成完整的朝向表示 |
| 5 | $v_{i,x}$ | $x$ 方向线速度 | 瞬时运动状态，用于加减速/转向决策 |
| 6 | $v_{i,y}$ | $y$ 方向线速度 | 同上 |
| 7 | $\omega_i$ | 偏航角速度（原始值，含符号） | 正值逆时针、负值顺时针，判断转弯状态 |
| 8 | $\|v_i^{xy}\|$ | 水平速率（$v_{x},v_{y}$ 的 L2 范数） | 标量速度大小，消除方向信息，用于速度极限判断 |
| 9 | $\|\omega_i\|$ | 绝对角速度 | 转弯剧烈程度的标量度量，用于稳定性评估 |

---

### 7.3 邻居共享状态 $o_i^{\text{nbr}}$（$3\times 7=21$ 维）

邻居状态基于通信半径 $R_{\text{comm}}$（默认 6.0 m）内的可见邻居计算。若 $R_{\text{comm}}\le0$，则暂时取消通信距离限制，所有非自身 rover 都视为可见；若可见邻居数 $|\mathcal{N}_i(t)|>3$，按距离取最近 3 个；若少于 3 个，不足槽位补零并由可见性掩码标记。

每个邻居槽位 $j$ 包含 7 维相对状态：

| 维度索引 | 记号 | 含义 | 物理意义 |
|:---:|---|---|---|
| 0 | $\Delta x_{ij}$ | 邻居相对 $x$ 偏移 | 邻居在自身右侧（正）还是左侧（负），用于避碰与编队 |
| 1 | $\Delta y_{ij}$ | 邻居相对 $y$ 偏移 | 邻居在自身前方（正）还是后方（负） |
| 2 | $\Delta v_{ij,x}$ | 相对 $x$ 速度 | 判断邻居在横向上是接近还是远离 |
| 3 | $\Delta v_{ij,y}$ | 相对 $y$ 速度 | 判断邻居在纵向上是接近还是远离，预判碰撞风险 |
| 4 | $\cos(\psi_j-\psi_i)$ | 相对航向余弦 | 邻居与自己同向（$\approx 1$）还是对向（$\approx -1$） |
| 5 | $\sin(\psi_j-\psi_i)$ | 相对航向正弦 | 与余弦配对完整描述相对朝向 |
| 6 | $m_{ij}$ | 可见性掩码 | $1.0$ 表示该槽位有真实邻居，$0.0$ 表示空槽（补零） |

3 个邻居槽位按距离升序排列后摊平为 21 维向量。

---

### 7.4 局部地形网格 $o_i^{\text{ter}}$（50 维）

局部地形网格从 procedural heightfield / crater proxy 中提取。固定车体系采样坐标为：

```text
forward_x_m = [-0.4, 0.0, 0.4, 0.8, 1.2]
lateral_y_m = [-0.8, -0.4, 0.0, 0.4, 0.8]
channels = [relative_height, risk]
```

每个采样点的相对高度为 $H(p_{\text{sample}})-H(p_i)$，风险为 $1-\tau(p_{\text{sample}})$。网格随 rover yaw 旋转到世界系查询，然后按 `x -> y -> channel` 顺序展平为 50 维。平坦地形下全部为 0。

原脚下 5 维 `height/slope_x/slope_y/roughness/traversability` 仍用于 proxy 动力学速度缩放和 terrain reward，但不再直接作为 actor 地形输入。地图面积可通过 `safety.world_xy_limit` 和 `terrain.crater_field_size` 扩大；例如新工程探针使用 `world_xy_limit=12.5`、`crater_field_size=25.0`，对应 $25\,\text{m}\times25\,\text{m}$ 区域。该地图尺度变化不会自动扩大 Actor 的局部地形网格，当前仍保持 50 维输入。

为了让扩大后的地图真正进入训练分布，reset 初始队形新增 `initial_state` 配置。默认仍保持旧实验的半径 $3\sim4\,\text{m}$、中心 $[-1,1]\,\text{m}$ 和 $0.35\,\text{m}$ jitter；新长训可显式扩大 spawn 半径和中心采样范围，但这只改变初始状态分布，不改变 Actor/Critic 接口。

`initial_state` 可启用训练期课程化：训练时从 `curriculum_start_*` 初始分布开始，经过 warmup/ramp 逐步过渡到目标分布；独立 eval 默认使用目标分布，避免把课程早期的简单 reset 当作 strict 结果。

---

### 7.5 局部聚合特征 $o_i^{\text{agg}}$（5 维）

聚合特征是对通信半径内所有可见邻居的整体统计，不针对某个特定邻居，为策略提供群体层面的"宏观感知"。

| 维度索引 | 记号 | 含义 | 物理意义 |
|:---:|---|---|---|
| 0 | $c_{i,x}^{\text{rel}}$ | 邻居质心相对 $x$ | 可见邻居几何中心相对于自身的 $x$ 偏移，"群体中心在哪" |
| 1 | $c_{i,y}^{\text{rel}}$ | 邻居质心相对 $y$ | 同上 $y$ 方向 |
| 2 | $\bar d_i^{\text{nbr}}$ | 平均邻距 | 到所有可见邻居的平均欧氏距离，大值=群体分散 |
| 3 | $d_i^{\text{min}}$ | 最近邻距离 | 到最近可见邻居的距离，碰撞避免的最直接信号 |
| 4 | $\sigma_i^{\text{nbr}}$ | 局部分散度 | 邻居位置围绕质心的空间方差，衡量通信范围内群体的散布程度 |

所有聚合指标均排除自身，仅使用通信半径掩码内的邻居计算；当 `communication_radius<=0` 时，该掩码退化为“除自身外全部可见”。

---

### 7.6 Oracle 信息边界

以下信息**刻意不进入** actor 观测：

- 最优采集点坐标 $p^{*}(t)$
- 各车到 $p^{*}$ 的距离 $d_i^{*}(t)$
- 平均 oracle 距离 $\bar d^{*}(t)$
- 质心-目标差距 $\|\bar p^{xy}(t)-p^{*xy}(t)\|$

这保证了 Dec-MARL 的信息边界：执行期 actor 仅依赖局部可观测信息进行去中心化决策，oracle 信息仅在训练期供 centralized critic、奖励塑形和评估指标使用。

---

## 8. critic 状态空间

### 8.1 总体结构

centralized critic 输入是一个全局扁平向量，形状为 $(N_{\text{env}}, d_s)$，其中 $d_s=54$。**该向量无 agent 维度**——critic 以整个团队为粒度进行价值估计。

critic 状态由四类特征沿最后一维拼接，拼接顺序固定：

$$
s(t)=
\left[
s^{\text{agent}}(t),\;
s^{\text{team}}(t),\;
s^{\text{ter}}(t),\;
s^{\text{oracle}}(t)
\right]
\in \mathbb{R}^{54}.
$$

维度构成：

$$
d_s = N \cdot d_{\text{agent}} + d_{\text{team}} + d_{\text{ter\_state}} + d_{\text{oracle}}
= 4 \times 8 + 8 + 5 + 9 = 54.
$$

| 分量 | 维度 | 含义 | critic 可见 | actor 可见 |
|---|---:|---|:---:|:---:|
| $s^{\text{agent}}$ | $4\times 8=32$ | 全部 4 车真值运动状态摊平 | 是 | 否 |
| $s^{\text{team}}$ | 8 | 团队几何形态与运动统计量 | 是 | 否 |
| $s^{\text{ter}}$ | 5 | 团队平均地形条件摘要 | 是 | 否 |
| $s^{\text{oracle}}$ | 9 | 最优集合点坐标及距离统计量 | 是 | 否 |

critic 状态通过 `MultiRoverGatheringCore.get_observations()` 在每个 reset 和 step 时刻同步构造。在 SKRL 训练流程中，价值网络 `SKRLValue` 以 $d_s=54$ 为输入维度，经三层 MLP $(54\to 128\to 128\to 1)$ 输出团队状态价值 $V_\phi(s(t))$。

---

### 8.2 全局智能体状态 $s^{\text{agent}}$（$4\times 8=32$ 维）

每车 8 维真值运动状态，4 辆车全部摊平拼接。注意与 actor ego（10 维）不同：critic 每车状态不含派生量 `speed_xy` 和 `abs_angular_velocity`，因为 critic 拥有全局视野，可直接从原始状态推理。

第 $i$ 辆车的 8 维状态：

| 维度索引 | 记号 | 含义 | 物理意义 |
|:---:|---|---|---|
| 0 | $x_i$ | 世界系 $x$ 坐标 | critic 以全局视角直接感知所有车辆位置 |
| 1 | $y_i$ | 世界系 $y$ 坐标 | 同上 |
| 2 | $z_i$ | 世界系 $z$ 坐标 | 车身高度，用于判断各车所处高程 |
| 3 | $\cos\psi_i$ | 航向角余弦 | 判断各车朝向一致性与对向冲突风险 |
| 4 | $\sin\psi_i$ | 航向角正弦 | 与余弦配对 |
| 5 | $v_{i,x}$ | $x$ 向线速度 | 判断整体运动趋势与有无车辆掉队 |
| 6 | $v_{i,y}$ | $y$ 向线速度 | 同上 |
| 7 | $\omega_i$ | 偏航角速度 | 各车机动状态评估 |

摊平顺序为 agent 0 至 agent 3 依次拼接，总长 $4\times 8=32$。

---

### 8.3 团队状态 $s^{\text{team}}$（8 维）

团队状态是对整个多车系统几何形态与运动特征的全局统计量，是 CTDE 架构中 critic 独有的全局视角。

| 维度索引 | 记号 | 含义 | 物理意义 |
|:---:|---|---|---|
| 0 | $D_{\max}$ | 最大 pairwise 距离 | 团队的极端展开程度：大值=有车脱离群体，小值=紧密聚集。衡量搜索覆盖与凝聚力平衡 |
| 1 | $\sigma_p^2$ | 团队分散度 | $\frac{1}{N}\sum_i\|p_i-\bar p\|^2$，均方距离度量，比 $D_{\max}$ 更平滑地反映"典型"分布 |
| 2 | $\bar p_x$ | 团队质心 $x$ | 团队几何中心的 $x$ 坐标，作为"团队位置代理" |
| 3 | $\bar p_y$ | 团队质心 $y$ | 同上 $y$ 方向 |
| 4 | $\bar p_z$ | 团队质心 $z$ | 团队平均高程 |
| 5 | $\bar d_{ij}$ | 平均 pairwise 距离 | $\frac{2}{N(N-1)}\sum_{i<j}\|p_i-p_j\|$，直接的车间距离平均。与 $\sigma_p^2$ 互补描述群体几何 |
| 6 | $\bar v$ | 团队平均速率 | $\frac{1}{N}\sum_i\|v_i^{xy}\|$，团队整体是高速行进还是低速搜索 |
| 7 | $t_{\text{hold}}$ | 成功保持计数 | 团队处于成功状态的累计时间步数，用于奖励塑形中的保持分量 |

---

### 8.4 全局地形状态 $s^{\text{ter}}$（5 维）

critic 对全部车辆的局部地形网格生成固定 5 维团队摘要。

| 维度索引 | 记号 | 含义 | 物理意义 |
|:---:|---|---|---|
| 0 | $\overline{|\Delta h|}$ | 平均绝对高差 | 团队周边地形总体起伏 |
| 1 | $\Delta h_{\max}^{+}$ | 最大上升 | 局部网格内最高上坡幅度 |
| 2 | $\Delta h_{\max}^{-}$ | 最大下降 | 局部网格内最大下陷幅度 |
| 3 | $\bar r_{\text{risk}}$ | 平均风险 | 团队周边平均通行风险 |
| 4 | $r_{\text{risk}}^{\max}$ | 最大风险 | 团队局部网格中的最危险区域 |

平坦地形（默认 `TerrainCfg`）下全部为 0。critic 总状态维度仍保持 54。

---

### 8.5 Oracle 特权特征 $s^{\text{oracle}}$（9 维）

这是 critic **独有**的训练期特权信息，是 CTDE 架构中"集中式训练"的核心。这些量在真实部署中**不可获取**，仅在仿真训练中用于引导策略学习。

| 维度索引 | 记号 | 含义 | 物理意义 |
|:---:|---|---|---|
| 0 | $p^{*}_x$ | oracle 点 $x$ 坐标 | 任务目标的真实世界坐标，critic 用于准确的优势估计 |
| 1 | $p^{*}_y$ | oracle 点 $y$ 坐标 | 同上 |
| 2 | $p^{*}_z$ | oracle 点 $z$ 坐标 | 目标点高程 |
| 3 | $d^{*}_1$ | agent 1 到 oracle 距离 | 判断哪辆车最接近目标（可能成为"领路者"） |
| 4 | $d^{*}_2$ | agent 2 到 oracle 距离 | 同上 |
| 5 | $d^{*}_3$ | agent 3 到 oracle 距离 | 同上 |
| 6 | $d^{*}_4$ | agent 4 到 oracle 距离 | 同上，识别严重偏离的车辆 |
| 7 | $\bar d^{*}$ | 平均 oracle 距离 | $\frac{1}{N}\sum_i d_i^{*}$，团队整体离目标的远近，单调递减=好信号 |
| 8 | $g_{\text{cent}}$ | 质心-目标差距 | $\|\bar p^{xy}-p^{*xy}\|$，与 $\bar d^{*}$ 互补：团队紧密聚集时 $g_{\text{cent}}\approx\bar d^{*}$；分散但质心恰好在目标上时 $g_{\text{cent}}\approx 0$ 而 $\bar d^{*}$ 可能很大 |

其中各车距离定义为 2D 水平欧氏距离：

$$
d_i^{*}(t)=\|p_i^{xy}(t)-p^{*xy}(t)\|.
$$

---

## 9. 动作空间

### 9.1 总体结构

actor 动作空间定义为低维局部子目标动作，类型为 `local_subgoal_polar`。多智能体动作张量形状为 $(N_{\text{env}}, N, d_a)$，其中 $d_a=2$。

$$
a_i(t)=
\left[
a_i^{\text{fwd}}(t),\;
a_i^{\text{turn}}(t)
\right]
\in [-1,1]^2.
$$

策略网络输出的是归一化后的连续动作，再经动作解释器转换为物理子目标。选择低维极坐标子目标而非高维轨迹骨架的设计动机为降低当前阶段的训练难度，避免策略直接学习轨迹序列的时序耦合。

| 属性 | 值 |
|---|---|
| 动作类型 | `local_subgoal_polar` |
| 单智能体动作维度 $d_a$ | 2 |
| 归一化值域 | $[-1,1]^2$ |
| Gym 动作空间 | `Box(low=-1.0, high=1.0, shape=(N, 2))` |
| SKRL 动作空间 | `Box(low=-1.0, high=1.0, shape=(2,))` per agent |

---

### 9.2 策略网络输出层

actor 网络采用参数共享的对角高斯策略。网络输出头给出 2 维均值向量并经 $\tanh$ 压缩至 $(-1,1)$：

$$
\mu_i(t) = \tanh\big(f_\theta(o_i(t))\big) \in (-1, 1)^2.
$$

对数标准差 $\log\sigma$ 为可学习参数，初始化为 $-0.5$（对应 $\sigma\approx 0.61$），所有智能体共享同一值。

动作采样分布为对角多元高斯：

$$
a_i(t) \sim \mathcal{N}\big(\mu_i(t),\; \text{diag}(\sigma^2)\big).
$$

采样后经 SKRL 的 `clip_actions` 机制将采样值截断至 $[-1, 1]$，确保动作始终落在有效范围内。旧兼容策略网络结构为 `mlp_v1`：

$$
\text{Input}(86) \to \text{Linear}(128) \to \text{ELU} \to \text{Linear}(128) \to \text{ELU} \to \text{Linear}(2) \to \tanh \to \mathcal{N}(\mu,\sigma^2).
$$

新工程路径 `branched_v1` 保持输入 86 和输出 2 不变，但把 observation 固定拆成 ego、neighbor、terrain 和 local-consensus / aggregation 四个编码分支，再接共享 MLP 主干：

$$
(10\to32)\oplus(21\to48)\oplus(50\to64)\oplus(5\to16)\to160\to128\to128\to2.
$$

---

### 9.3 动作解释管线

归一化动作 $a_i(t) \in [-1,1]^2$ 经四步解释为世界系局部子目标点。管线由 `DecodedAction` 数据类承载中间结果。

**第一步：裁剪**

对输入动作做防御性裁剪，确保在 $[-1,1]$ 范围内：

$$
\tilde a_i(t) = \text{clip}(a_i(t), -1, 1).
$$

**第二步：归一化-物理尺度映射**

将 $[-1,1]$ 归一化值映射到物理极坐标。前向通道采用线性映射将 $[-1,1]$ 拉伸至 $[0, \rho_{\max}]$，转向通道将 $[-1,1]$ 缩放至 $[-\beta_{\max}, \beta_{\max}]$：

$$
\rho_i(t) = \frac{\tilde a_i^{\text{fwd}}(t) + 1}{2} \cdot \rho_{\max},
$$

$$
\beta_i(t) = \tilde a_i^{\text{turn}}(t) \cdot \beta_{\max}.
$$

前向通道的偏置设计保证即使策略输出 $-1$（最小前向），$\rho_i$ 仍为 $0$ 而非负值；策略输出 $+1$ 时达到最大前向距离 $\rho_{\max}$。转向通道的对称设计使 $-1$ 对应最大右转（$-\beta_{\max}$），$+1$ 对应最大左转（$+\beta_{\max}$）。

**第三步：极坐标-局部笛卡尔转换**

将物理极坐标 $(\rho_i, \beta_i)$ 转换为车体系下的局部笛卡尔子目标：

$$
p_{i,\text{sub}}^{b}(t)=
\begin{bmatrix}
\rho_i(t)\cos\beta_i(t) \\
\rho_i(t)\sin\beta_i(t)
\end{bmatrix}.
$$

**第四步：局部-世界坐标系旋转**

将车体系子目标绕 $z$ 轴旋转 $\psi_i$，再加到当前世界位置，得到世界系子目标：

$$
p_{i,\text{sub}}^{w}(t) = p_i^{xy}(t) + R_z(\psi_i)\; p_{i,\text{sub}}^{b}(t),
$$

其中旋转矩阵为：

$$
R_z(\psi_i) = \begin{bmatrix}
\cos\psi_i & -\sin\psi_i \\
\sin\psi_i & \cos\psi_i
\end{bmatrix}.
$$

若需要高程信息，由局部地形函数补全；在高保真评估中由 Isaac/PhysX 场景查询对应地形高度：

$$
z_{i,\text{sub}} = H(x_{i,\text{sub}}, y_{i,\text{sub}}).
$$

| 管线步骤 | 输入 | 输出 | 坐标空间 | 核心操作 |
|---|---|---|---|---|
| 裁剪 | $a_i\in\mathbb{R}^2$ | $\tilde a_i\in[-1,1]^2$ | 归一化 | $\text{clip}(\cdot,-1,1)$ |
| 尺度映射 | $\tilde a_i$ | $(\rho_i,\beta_i)$ | 极坐标（车体系） | 线性映射 + 偏置缩放 |
| 极坐标-笛卡尔 | $(\rho_i,\beta_i)$ | $(x_i^b, y_i^b)$ | 笛卡尔（车体系） | $\rho\cos\beta,\;\rho\sin\beta$ |
| 世界系变换 | $(x_i^b, y_i^b)$ | $p_{i,\text{sub}}^{w}$ | 笛卡尔（世界系） | 旋转 $R_z(\psi_i)$ + 平移 $p_i$ |

---

### 9.4 动作字段定义

| 维度索引 | 记号 | 归一化值域 | 物理值域 | 坐标系 | 物理意义 |
|:---:|---|---|---|---|---|
| 0 | $a_i^{\text{fwd}}$ | $[-1,1]$ | $[0,\rho_{\max}]$ | 车体系极坐标 | 控制局部子目标的径向距离。$-1$ 表示原地不动（$\rho=0$），$+1$ 表示最大前进（$\rho=\rho_{\max}$）。决定策略在局部规划窗口内的探索/利用半径 |
| 1 | $a_i^{\text{turn}}$ | $[-1,1]$ | $[-\beta_{\max},\beta_{\max}]$ | 车体系极坐标 | 控制局部子目标相对于车头的方位角。$-1$ 表示最大右转，$0$ 表示直行，$+1$ 表示最大左转。决定策略的运动方向偏好 |

---

### 9.5 配置参数

动作空间的可配置参数定义于 `PlannerCfg`：

| 参数 | 默认值 | 含义 | 可通过 YAML 覆盖 |
|---|---|---|---|
| `action_type` | `"local_subgoal_polar"` | 动作类型标识 | 否 |
| `action_dim` | 2 | 动作维度 | 否 |
| `rho_max` | 1.2 m | 局部子目标最大径向距离 | 是 |
| `beta_max` | $\pi/2$ (90°) | 局部子目标最大方位角 | 是 |

`rho_max` 和 `beta_max` 可通过实验 YAML 中的 `planner` 节覆盖。不同实验配置中已使用过的典型值：

| $\rho_{\max}$ (m) | $\beta_{\max}$ (deg) | 设计意图 |
|---|---|---|
| 1.2 | 90 | 默认配置，较大动作空间 |
| 0.8 | 60 | 中等约束 |
| 0.6 | 45 | 较强约束，抑制过大转向 |
| 0.5 | 30 | 强约束，短视距小转向 |

`rho_max` 的默认值 1.2 m 约为月球车 1–2 个规划周期的行进距离，限制策略在每个决策步内的最大位移；`beta_max` 的默认值 $90^\circ$ 允许策略在单步内大幅改变方向。通过收缩这两个值可约束策略的动作幅度，降低探索难度。

动作网络输出层参数 $\log\sigma$ 初始化为 $-0.5$，不可通过 YAML 配置，仅在 checkpoint 中持久化。

---

## 10. 轨迹生成器

### 10.1 总体结构

轨迹生成器接收动作解释器输出的世界系局部子目标 $p_{i,\text{sub}}^{w}(t)$，在当前位置与子目标之间插值生成局部参考轨迹：

$$
G_{\text{traj}}:\ p_{i,\text{sub}}^{w}(t) \mapsto \mathcal{T}_i(t).
$$

轨迹生成器采用确定性规则，不由 RL 学习速度剖面和轨迹曲率。旧配置使用 `line` 直线段插值；新工程探针可切换为 `quintic` 2D quintic Hermite 曲线。

### 10.2 轨迹生成流程

1. 以当前世界位置为起点、世界系子目标 $p_{i,\text{sub}}^{w}$ 为终点，生成含 $K$ 个路径点（默认 $K=8$）的确定性局部路径；
2. 查询地形高度或局部高程函数，补全各路径点的 $z$ 坐标；
3. 沿路径方向插值各路径点的参考航向角 $\psi_{i,k}$；
4. 按固定参考速度 $v^{\text{ref}}$ 均分时间戳 $t_{i,k}$；
5. 根据限速规则和地形代价调整各路径点的参考速度 $v_{i,k}$。

### 10.3 输出轨迹

轨迹生成器输出带时间戳的局部参考轨迹：

$$
\mathcal{T}_i(t)=
\{(x_{i,k}, y_{i,k}, z_{i,k}, \psi_{i,k}, t_{i,k}, v_{i,k}^{\text{ref}})\}_{k=1}^{K},
$$

其中 $K$ 为路径点数量（默认 8），$v_{i,k}^{\text{ref}}$ 为第 $k$ 个路径点的参考前向速度。

| 字段 | 含义 |
|---|---|
| $x_{i,k}, y_{i,k}, z_{i,k}$ | 第 $k$ 个路径点的世界坐标 |
| $\psi_{i,k}$ | 第 $k$ 个路径点的参考航向角 |
| $t_{i,k}$ | 第 $k$ 个路径点的到达时间戳 |
| $v_{i,k}^{\text{ref}}$ | 第 $k$ 个路径点的参考前向速度 |

### 10.4 路径生成方式

`line` 方法采用直线段插值。在当前位置 $(x_i, y_i)$ 与子目标 $(x_{i,\text{sub}}, y_{i,\text{sub}})$ 之间等距采样 $K$ 个路径点，路径点索引 $k=1,\dots,K$：

$$
(x_{i,k}, y_{i,k}) = (x_i, y_i) + \frac{k}{K} \cdot (x_{i,\text{sub}} - x_i,\; y_{i,\text{sub}} - y_i).
$$

`quintic` 方法采用 2D quintic Hermite 曲线：起点为当前 rover $xy$，起点切向由当前 yaw 给出，终点为 decoded / filtered subgoal $xy$，终点切向默认指向 subgoal 方向，起终点二阶导设为 0，以减少参考轨迹突变。该方法只替换 proxy 参考轨迹几何，不改变 `Trajectory.points/headings/timestamps/reference_speed` 接口。

---

## 11. 简化控制执行层

### 11.1 总体结构

由于月球车底层控制接口尚未确定，当前方案采用简化比例速度跟踪控制器。控制器接收轨迹生成器输出的局部参考轨迹 $\mathcal{T}_i(t)$，输出车体速度命令：

$$
u_i(t)=
\left[
v_i^{\text{cmd}}(t),\;
\omega_i^{\text{cmd}}(t)
\right],
$$

其中 $v_i^{\text{cmd}}$ 为期望前向速度，$\omega_i^{\text{cmd}}$ 为期望偏航角速度。

### 11.2 跟踪点选取

控制器取轨迹上索引 $k=1$ 的路径点（第一个非当前位置的路径点）作为跟踪目标。设跟踪目标点位置为 $(x_i^{\text{ref}}, y_i^{\text{ref}})$，参考航向为 $\psi_i^{\text{ref}}$。

### 11.3 比例控制律

使用比例控制器分别计算前向速度和偏航角速度：

$$
v_i^{\text{cmd}}(t) = k_v \cdot d_i(t),
$$

$$
\omega_i^{\text{cmd}}(t) = k_\omega \cdot \Delta\psi_i(t),
$$

其中 $k_v$ 为线速度比例增益，$k_\omega$ 为角速度比例增益，

$$
d_i(t) = \left\| (x_i^{\text{ref}} - x_i,\; y_i^{\text{ref}} - y_i) \right\|
$$

为到跟踪点的水平距离，

$$
\Delta\psi_i(t) = \text{atan2}(\sin(\psi_i^{\text{ref}} - \psi_i),\; \cos(\psi_i^{\text{ref}} - \psi_i))
$$

为航向偏差（已处理角度环绕）。控制命令经地形速度缩放后进入 2D/2.5D proxy 运动学状态更新。

控制器为纯比例控制，不包含前馈补偿或模型预测。

### 11.4 Proxy 状态更新

控制命令在 proxy 训练中直接驱动运动学积分。旧兼容路径 `unicycle` 将 $[v_i^{\text{cmd}},\omega_i^{\text{cmd}}]$ 按控制周期 $\Delta t_c$ 施加于 unicycle 模型：

$$
\psi_i(t+\Delta t_c)=\psi_i(t)+\omega_i^{\text{cmd}}(t)\Delta t_c,
$$

$$
p_i^{xy}(t+\Delta t_c)=p_i^{xy}(t)+
\begin{bmatrix}
\cos \psi_i(t+\Delta t_c)\\
\sin \psi_i(t+\Delta t_c)
\end{bmatrix}
v_i^{\text{cmd}}(t)\Delta t_c.
$$

两种模型都使用地形速度缩放。地形 proxy 开启时，线速度按可通过性 $\tau_i$ 和坡度进行缩放：

$$
v_i^{\text{cmd},\text{scaled}}(t) = v_i^{\text{cmd}}(t) \cdot \tau_i \cdot \exp(-k_s \cdot \|\nabla H_i\|),
$$

新工程路径 `bicycle` 保留同一个控制器输出，但在 `_integrate()` 中先把 yaw-rate demand 转为转角：

$$
\delta_i=\text{clip}\left(
\arctan\frac{L\omega_i^{\text{cmd}}}{\max(v_i^{\text{cmd}},\epsilon)},
-\delta_{\max},\delta_{\max}
\right),
$$

再使用 terrain speed scaling 后的前向速度 $v_i^{\text{eff}}$ 计算实际 yaw-rate：

$$
\omega_i^{\text{actual}}=
\frac{v_i^{\text{eff}}}{L}\tan\delta_i.
$$

位置更新使用半隐式 midpoint heading；`angular_velocities` 记录 $\omega_i^{\text{actual}}$，step info / telemetry 记录 `steering_angle`、`actual_yaw_rate` 和 `turning_radius`。该 bicycle v1 不引入质量、惯量、转向速率状态、轮地接触、悬挂或滑移模型。

并用地形高度函数更新 $z$ 坐标。状态更新不包含质量、惯量、轮地接触、打滑、悬挂或 PhysX contact。

### 11.5 高保真评估适配

高保真评估中，控制适配器根据轮式资产接口，将 $[v_i^{\text{cmd}},\omega_i^{\text{cmd}}]$ 转换为 Isaac/PhysX 可执行的控制目标。可选接口包括：

1. 左右轮速度目标；
2. 转向角 + 前向速度目标；
3. 关节速度目标；
4. 关节力矩目标；
5. 简化底盘控制目标。

当前技术方案不把 articulation 控制接口作为训练主路径的一部分；它属于高保真评估和后续真实 rover asset 迁移时需要冻结的适配层。

---

## 12. 集合成功判据

集合成功采用纯几何聚集判据。设：

$$
\bar p(t)=\frac{1}{N}\sum_{i=1}^{N}p_i(t),
$$

$$
D_{\max}(t)=\max_{i,j}\|p_i(t)-p_j(t)\|,
$$

$$
\sigma_p^2(t)=\frac{1}{N}\sum_{i=1}^{N}\|p_i(t)-\bar p(t)\|^2.
$$

若在连续 $n_{\text{hold}}$ 个判定时刻内同时满足：

$$
D_{\max}(t)\le \varepsilon_D,
$$

$$
\sigma_p^2(t)\le \varepsilon_\sigma,
$$

$$
\|v_i(t)\|\le \varepsilon_v,\quad i=1,\dots,N,
$$

则判定集合成功。

其中，$\varepsilon_D$ 为最大 pairwise 距离阈值，$\varepsilon_\sigma$ 为团队分散度阈值，$\varepsilon_v$ 为速度阈值，$n_{\text{hold}}$ 为连续保持步数。

---

## 13. 奖励函数

总奖励定义为：

$$
r_t
=
w_g r_{\text{gather}}(t)
+
w_o r_{\text{oracle}}(t)
+
w_e r_{\text{energy}}(t)
+
w_s r_{\text{safety}}(t)
+
w_m r_{\text{motion}}(t)
+
w_c r_{\text{consistency}}(t)
+
w_T r_{\text{terminal}}(t).
$$

### 13.1 自组织聚集奖励

$$
r_{\text{gather}}(t)
=
\alpha_1\big(D_{\max}(t-1)-D_{\max}(t)\big)
+
\alpha_2\big(\sigma_p^2(t-1)-\sigma_p^2(t)\big).
$$

该项衡量团队几何收缩趋势，不依赖显式集合点。

### 13.2 oracle 辅助奖励

定义平均最优点距离：

$$
\bar d^{*}(t)
=
\frac{1}{N}\sum_{i=1}^{N}\|p_i(t)-p^{*}(t)\|.
$$

oracle 辅助奖励固定采用平均距离下降量：

$$
r_{\text{oracle}}(t)
=
\alpha_3\big(\bar d^{*}(t-1)-\bar d^{*}(t)\big).
$$

该项仅用于训练塑形，不作为执行期输入。

### 13.3 能耗代理奖励

当前训练主路径采用代理项：

$$
r_{\text{energy}}(t)
=
-\alpha_4 L(t)
-\alpha_5 C_{\text{slope}}(t)
-\alpha_6 C_{\text{turn}}(t)
-\alpha_7 C_{\text{terrain}}(t).
$$

其中，$L(t)$ 为路径长度或位移代价，$C_{\text{slope}}$ 为坡度代价，$C_{\text{turn}}$ 为转向代价，$C_{\text{terrain}}$ 为高风险地形代价。

在具备关节力矩和关节速度后，可扩展为物理能耗近似：

$$
C_{\text{energy}}(t)
=
\sum_{\tau=t}^{t+\Delta t_p}
\sum_q
|\tau_q(\tau)\dot q(\tau)|\Delta t_c.
$$

该扩展不属于当前 proxy 训练主路径的必要条件。

### 13.4 安全惩罚

$$
r_{\text{safety}}(t)
=
-\alpha_8 C_{\text{obs}}(t)
-\alpha_9 C_{\text{agent}}(t)
-\alpha_{10} C_{\text{near}}(t).
$$

其中，$C_{\text{obs}}$ 为障碍碰撞惩罚，$C_{\text{agent}}$ 为车间碰撞惩罚，$C_{\text{near}}$ 为危险近距惩罚。

### 13.5 运动质量奖励

由于当前动作空间已降为 $[\rho,\beta]$，运动质量项简化为：

$$
r_{\text{motion}}(t)
=
-\alpha_{11}C_{\text{turn}}(t)
-\alpha_{12}C_{\text{subgoal}}(t).
$$

其中，$C_{\text{turn}}(t)$ 用于抑制过大局部转向，$C_{\text{subgoal}}(t)$ 用于抑制长期输出过小 $\rho_i$ 导致的停滞行为。

### 13.6 一致性奖励

$$
r_{\text{consistency}}(t)
=
-\alpha_{14}
\sum_{i=1}^{N}
\left\|
\begin{bmatrix}
\rho_i(t)\\
\beta_i(t)
\end{bmatrix}
-
\begin{bmatrix}
\rho_i(t-1)\\
\beta_i(t-1)
\end{bmatrix}
\right\|_2^2.
$$

### 13.7 终端奖励

$$
r_{\text{terminal}}(t)=
\begin{cases}
+R_{\text{succ}}, & \text{集合成功},\\
-R_{\text{fail}}, & \text{碰撞/越界/超时},\\
0, & \text{其他}.
\end{cases}
$$

---

## 14. 网络结构

### 14.1 actor 网络

actor 采用参数共享结构。每个智能体共享同一套策略网络参数。输入为局部观测 $o_i(t)$，输出为 2 维连续动作：

$$
a_i(t)=[\rho_i(t),\beta_i(t)].
$$

actor 由四个编码分支和一个共享 MLP 主干组成：

| 分支 | 输入切片 | 维度 | 编码维度 | 作用 |
|---|---|---:|---:|---|
| ego encoder | `[0:10]` | 10 | 32 | 编码自车状态 |
| neighbor encoder | `[10:31]` | 21 | 48 | 编码邻居共享状态 |
| terrain encoder | `[31:81]` | 50 | 64 | 编码局部地形网格 |
| local-consensus encoder | `[81:86]` | 5 | 16 | 编码局部聚集态势 |

四个分支 concat 后为 160 维，经共享主干 $160\to128\to128\to2$ 输出动作均值。旧 `mlp_v1` 仍保留为 checkpoint 兼容路径。

输出头为连续动作分布参数。动作采样后通过有界映射约束到：

$$
\rho_i\in[0,\rho_{\max}],
\quad
\beta_i\in[-\beta_{\max},\beta_{\max}].
$$

### 14.2 critic 网络

critic 采用 centralized value network。输入为全局状态 $s(t)$，输出团队共享状态价值：

$$
V_\phi(s(t)).
$$

critic 输入包含全部智能体真值状态、团队几何统计量、地形摘要和 oracle 辅助信息。旧 `mlp_v1` 仍保留为 checkpoint 兼容路径；新 `structured_v1` 固定拆分 54 维 centralized state：

| 分支 | 输入切片 | 维度 | 处理方式 |
|---|---|---:|---|
| agent states | `[0:32]` | $4\times8$ | 共享 agent encoder 后做 mean + max 聚合 |
| team stats | `[32:40]` | 8 | 独立分支 |
| terrain summary | `[40:45]` | 5 | 独立分支 |
| oracle state | `[45:54]` | 9 | 独立分支 |

各分支 concat 后接 value trunk $128\to128\to1$ 输出团队共享状态价值。

---

## 15. SKRL-MAPPO 训练流程

训练流程包括：

1. proxy core 创建并行多智能体任务环境；
2. 每个环境包含 4 个 rover 智能体；
3. actor 根据局部观测输出 $[\rho,\beta]$；
4. proxy 环境内部将动作转换为参考轨迹和控制命令；
5. proxy 运动学模型推进状态；
6. proxy 环境计算奖励、终止标志与下一观测；
7. SKRL 收集 rollout；
8. MAPPO 使用 centralized critic 计算优势并更新 actor/critic。

训练脚本可以通过 SKRL 的 `isaaclab-multi-agent` wrapper 接入多智能体接口，但该 wrapper 是数据接口适配层，不表示训练 loop 运行了 `SimulationApp`、PhysX 接触求解或真实 rover articulation。

---

## 16. 评估指标

评估指标分为 proxy 训练评估和 Isaac/PhysX 高保真闭环评估两类。

Proxy 任务指标包括集合成功率、平均完成时间、最大 pairwise 距离、团队分散度、平均邻距、碰撞率、超时率和 `dmax_reduction_ratio`。

oracle 最优性指标包括最终平均最优点距离 $\bar d^{*}(T)$、最终最大最优点距离、团队质心到 $p^{*}(T)$ 的距离和累计 oracle gap。

轨迹指标包括局部子目标变化幅度、轨迹长度、转向幅度、轨迹生成失败率和相邻规划周期动作差异。

Proxy strict gate 默认采用：

```text
dmax_reduction_ratio <= 0.2
success_rate >= 0.9
collision_rate <= 0.02
timeout_rate == 0
```

Isaac/PhysX 高保真闭环评估指标包括：

```text
rmse_cross_track_m
max_cross_track_m
path_completion_ratio
max_tilt_deg
collision / rollover / timeout diagnostics
```

高保真评估结果应表述为 checkpoint 或参考轨迹的 PhysX closed-loop validation，不应表述为训练 loop 已经在 Isaac Sim / PhysX 中完成。

---

## 17. 技术方案冻结结论

当前技术路线冻结为：

1. 采用 torch-vectorized proxy 环境作为 MAPPO/PPO 采样、reward 调试和 checkpoint selection 的训练主路径；
2. 采用 Isaac Sim / Isaac Lab / PhysX 作为 checkpoint 级高保真闭环评估平台；
3. 执行阶段不显式提供集合点；
4. 通信机制采用邻居状态共享；
5. actor 输出低维局部子目标动作 $[\rho,\beta]$；
6. 轨迹由确定性轨迹生成器生成；
7. proxy 训练中采用简化速度跟踪控制器和可配置的 2D/2.5D kinematic unicycle / bicycle 状态更新；
8. critic 训练期接收全局状态与 oracle 辅助信息；
9. oracle 辅助奖励采用平均距离下降量；
10. 高保真评估中通过轮式资产控制适配器验证轨迹跟踪、姿态稳定性、碰撞和地形通过性；
11. 结果表述必须区分 proxy strict pass、PhysX closed-loop evaluation 和真实 Isaac 物理训练。

---

## 18. 技术边界

| 技术边界 | 当前处理方式 | 含义 |
|---|---|---|
| 训练动力学 | 2D/2.5D kinematic unicycle / bicycle proxy | 不包含质量、惯量、悬挂、轮壤接触或 PhysX contact |
| 高保真评估 | Isaac/PhysX 轮式资产闭环跟踪 | 用于迁移 sanity check 和失败分析，不参与每次策略更新 |
| 当前轮式资产 | Jackal placeholder | 可验证控制链路，但不能代表最终 lunar rover asset |
| 地形输入 | 车体系 $5\times5\times2$ 结构化网格 | 视觉和点云当前不进入 policy input；PhysX 同布局扫描尚未接入 |
| Oracle 信息 | critic / reward / metrics 可用，actor 不可见 | 保持 centralized training、decentralized execution 边界 |
| 动作接口 | $[\rho,\beta]$ 局部子目标 | 暂不把高维轨迹骨架作为 actor 直接输出 |

---
