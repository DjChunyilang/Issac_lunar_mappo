# 多月球车自组织集合局部参考轨迹规划技术文档
## 代理训练与 Isaac/PhysX 高保真评估路线

> 本文只描述多月球车自组织集合任务的技术方案。当前实现口径已经从原 V2.0 的“Isaac Sim / Isaac Lab 作为主要训练平台”修订为“高吞吐 proxy 训练 + Isaac Sim / Isaac Lab / PhysX 高保真闭环评估”。工程目录、实施排期、运行命令和当前实验状态分别见 `docs/scaffold.md`、`docs/implementation_plan.md`、`docs/runbooks/` 和 `docs/current_status.md`。

---

## 1. 文档范围

本文档用于定义多月球车自组织集合任务的技术方案，包括任务建模、信息边界、强化学习建模方式、观测空间、critic 状态空间、动作空间、轨迹生成流程、简化控制链路、奖励函数、网络结构、训练闭环和评估判据。

当前训练主路径采用 PyTorch / torch-vectorized proxy 环境和 SKRL-MAPPO。Isaac Sim / Isaac Lab / PhysX 当前不参与每次 PPO/MAPPO 采样更新，而用于 checkpoint 级高保真闭环评估、迁移 sanity check、失效分析和可视化展示。Actor 不直接输出高维轨迹点序列，而输出低维局部子目标动作；局部参考轨迹由确定性轨迹生成器生成，proxy 训练中由简化速度控制器和 2D/2.5D 运动学模型推进状态，高保真评估中再通过 PhysX 轮式资产控制适配器验证轨迹跟踪与姿态稳定性。

---

## 2. 任务定义

设系统包含 $N=4$ 个同构月球车。第 $i$ 个智能体在时刻 $t$ 的状态记为 $x_i(t)$，位置为 $p_i(t)\in\mathbb{R}^3$，速度为 $v_i(t)$，航向角为 $\psi_i(t)$。

任务目标是在月面 2.5D/3D 地形条件下，使多个智能体通过局部感知与邻域通信完成自组织集合。执行阶段不向智能体显式提供集合点。智能体仅依据自身状态、邻居状态共享信息、局部地形特征和局部聚集态势进行分散决策。

训练阶段允许引入外部算法给出的最优集合点 $p^{*}(t)$。该点仅用于 centralized critic、oracle 辅助奖励和评估指标计算，不作为 actor 的观测输入。该设计保持集中式训练、分散式执行的信息边界。

当前阶段不考虑机械拼接与精细对接，仅以几何聚集成功作为任务完成标准。

---

## 3. 当前总体技术路线

主技术路线定义为：

$$
\text{Proxy Vectorized Training}
+
\text{SKRL-MAPPO}
+
\text{低维局部子目标动作}
+
\text{确定性轨迹生成器}
+
\text{简化速度跟踪控制器}
+
\text{Isaac/PhysX Checkpoint Evaluation}.
$$

与原 V2.0 表述相比，关键变更如下：

| 维度 | 原 V2.0 表述 | 当前技术路线 |
|---|---|---|
| 训练主路径 | Isaac Lab / PhysX 多车物理训练 | torch-vectorized proxy 环境 |
| 高保真仿真 | 训练 loop 的物理推进层 | checkpoint 级闭环评估层 |
| 动作接口 | 低维局部子目标 $[\rho,\beta]$ | 保持不变 |
| 轨迹与控制 | 子目标 -> 轨迹 -> 简化控制 | proxy 中用于运动学推进，PhysX 中通过轮式资产适配器验证 |
| 结果声明 | Isaac 物理训练成功 | proxy strict pass + PhysX closed-loop evaluation |

系统分为五个技术层。

第一层为 **proxy 多智能体任务层**。该层负责多环境并行采样、reset / step、观测构造、动作解释、奖励计算、终止判定、oracle 辅助信息计算和 metrics 记录。当前 proxy 动力学是 2D/2.5D kinematic unicycle 风格模型；地形开启时查询 procedural heightfield / crater proxy 特征并施加速度缩放，但不包含质量、惯量、轮地接触、打滑、沉陷、悬挂或 PhysX contact。

第二层为 **SKRL-MAPPO 训练层**。该层实现 centralized training with decentralized execution。Actor 执行期仅接收局部观测，critic 训练期接收全局状态和 oracle 辅助信息。`isaaclab-multi-agent` wrapper 只是 SKRL 接口适配层，不代表训练 loop 运行在 Isaac Sim / PhysX 中。

第三层为 **动作解释与轨迹生成层**。该层将 actor 输出的低维局部子目标动作 $[\rho_i,\beta_i]$ 转换为局部参考轨迹 $\mathcal{T}_i(t)$。

第四层为 **简化控制与 proxy 状态推进层**。该层将局部参考轨迹转换为车体速度命令 $[v_i^{\text{cmd}},\omega_i^{\text{cmd}}]$，再通过运动学积分更新 rover proxy state。

第五层为 **Isaac/PhysX 高保真评估层**。该层低频接收候选 checkpoint 或参考轨迹，在 Isaac Sim / Isaac Lab / PhysX 场景中执行闭环轨迹跟踪，报告 tracking error、path completion、tilt、碰撞和失败案例。当前使用 Jackal 作为轮式资产 placeholder；它可验证控制链路和评估流程，但不能等同于最终月球车低重力轮壤动力学。

---

## 4. 符号约定

| 符号 | 含义 |
|---|---|
| $N$ | 智能体数量，当前固定为 4 |
| $i,j$ | 智能体索引 |
| $t$ | 离散规划时刻 |
| $\Delta t_p$ | 规划刷新周期 |
| $\Delta t_c$ | 控制执行周期 |
| $x_i(t)$ | 第 $i$ 个智能体全状态 |
| $p_i(t)\in\mathbb{R}^3$ | 第 $i$ 个智能体位置 |
| $v_i(t)$ | 第 $i$ 个智能体速度 |
| $\psi_i(t)$ | 第 $i$ 个智能体航向角 |
| $o_i(t)$ | 第 $i$ 个智能体局部观测 |
| $s(t)$ | centralized critic 输入状态 |
| $a_i(t)$ | 第 $i$ 个智能体动作 |
| $p^{*}(t)$ | 外部算法给出的最优集合点 |
| $d_i^{*}(t)$ | 第 $i$ 个智能体到 $p^{*}(t)$ 的距离 |
| $\bar d^{*}(t)$ | 平均最优点距离 |
| $\bar p(t)$ | 团队几何中心 |
| $D_{\max}(t)$ | 团队最大 pairwise 距离 |
| $\sigma_p^2(t)$ | 团队分散度 |
| $\mathcal{T}_i(t)$ | 第 $i$ 个智能体的局部参考轨迹 |
| $K$ | 轨迹生成器输出的轨迹点数量 |

其中：

$$
\bar p(t)=\frac{1}{N}\sum_{i=1}^{N}p_i(t),
$$

$$
D_{\max}(t)=\max_{i,j}\|p_i(t)-p_j(t)\|,
$$

$$
\sigma_p^2(t)=\frac{1}{N}\sum_{i=1}^{N}\|p_i(t)-\bar p(t)\|^2,
$$

$$
d_i^{*}(t)=\|p_i(t)-p^{*}(t)\|,
$$

$$
\bar d^{*}(t)=\frac{1}{N}\sum_{i=1}^{N}d_i^{*}(t).
$$

---

## 5. 系统运行闭环

训练闭环定义为：

$$
\text{proxy state}
\rightarrow
\text{actor observation / critic state}
\rightarrow
\text{SKRL-MAPPO actor 输出低维动作}
\rightarrow
\text{动作解释器生成局部子目标}
\rightarrow
\text{轨迹生成器生成局部参考轨迹}
\rightarrow
\text{简化速度跟踪控制器生成控制命令}
\rightarrow
\text{proxy unicycle 状态更新}
\rightarrow
\text{奖励、终止与下一观测计算}.
$$

在每个规划步中，proxy 环境读取所有 rover 的状态、地形特征、邻居关系和安全事件，构造 actor 局部观测 $o_i(t)$ 与 critic 全局状态 $s(t)$。Actor 输出低维局部子目标动作后，动作解释器将其转换为局部参考轨迹，并通过简化控制器转换为速度命令。Proxy core 使用运动学积分推进状态，再计算奖励、终止标志和下一时刻观测。

高保真评估闭环定义为：

$$
\text{候选 checkpoint / 参考轨迹}
\rightarrow
\text{Isaac/PhysX 轮式资产控制适配}
\rightarrow
\text{PhysX 场景闭环推进}
\rightarrow
\text{tracking error / completion / tilt / failure metrics}.
$$

该闭环不参与每次 MAPPO 梯度更新，只用于 checkpoint 级验证和迁移风险诊断。

---

## 6. 信息边界

### 6.1 执行期信息

执行阶段，第 $i$ 个智能体可访问：

$$
o_i(t)=
\left[
o_i^{\text{ego}}(t),
o_i^{\text{nbr}}(t),
o_i^{\text{ter}}(t),
o_i^{\text{agg}}(t)
\right].
$$

其中，$o_i^{\text{ego}}(t)$ 为自车状态，$o_i^{\text{nbr}}(t)$ 为邻居状态共享信息，$o_i^{\text{ter}}(t)$ 为局部地形手工特征，$o_i^{\text{agg}}(t)$ 为局部聚集态势特征。

执行期 actor 不可访问 $p^{*}(t)$、$d_i^{*}(t)$、$\bar d^{*}(t)$ 等 oracle 信息。

### 6.2 训练期信息

训练阶段 centralized critic 可访问：

$$
s(t)=
\left[
s^{\text{agent}}(t),
s^{\text{team}}(t),
s^{\text{ter}}(t),
s^{\text{oracle}}(t)
\right].
$$

其中，$s^{\text{agent}}(t)$ 为全部智能体真值状态，$s^{\text{team}}(t)$ 为团队几何统计量，$s^{\text{ter}}(t)$ 为地形摘要，$s^{\text{oracle}}(t)$ 为外部最优集合点及其距离统计量。

### 6.3 通信机制

当前阶段通信机制采用邻居状态共享。设第 $i$ 个智能体的通信邻居集合为 $\mathcal{N}_i(t)$，则其可接收 $\mathcal{N}_i(t)$ 内邻居的状态摘要。

---

## 7. 观测空间

actor 观测空间定义为：

$$
o_i(t)=
\left[
o_i^{\text{ego}}(t),
o_i^{\text{nbr}}(t),
o_i^{\text{ter}}(t),
o_i^{\text{agg}}(t)
\right].
$$

| 分量 | 内容 | 来源 | actor 可见 |
|---|---|---|---|
| $o_i^{\text{ego}}$ | 自车位置、姿态、速度、角速度、底盘姿态代理量 | proxy rover state；高保真评估时来自 PhysX state | 是 |
| $o_i^{\text{nbr}}$ | 邻居相对位置、相对速度、相对航向、可见性 mask | proxy 环境按通信半径计算 | 是 |
| $o_i^{\text{ter}}$ | 坡度、粗糙度、高差、障碍密度、可通行宽度 | procedural heightfield / crater proxy 特征；高保真评估时来自 PhysX 场景诊断 | 是 |
| $o_i^{\text{agg}}$ | 邻域质心、平均邻距、最近邻距离、局部分散度 | 邻居状态统计 | 是 |
| $p^{*}$ 相关量 | 最优集合点及距离统计 | 外部算法 | 否 |



---

## 8. critic 状态空间

centralized critic 输入定义为：

$$
s(t)=
\left[
s^{\text{agent}}(t),
s^{\text{team}}(t),
s^{\text{ter}}(t),
s^{\text{oracle}}(t)
\right].
$$

| 分量 | 内容 | critic 可见 | actor 可见 |
|---|---|---|---|
| $s^{\text{agent}}$ | 4 个 rover 的真值状态 | 是 | 否 |
| $s^{\text{team}}$ | $D_{\max}$、$\sigma_p^2$、$\bar p$、平均邻距等 | 是 | 否 |
| $s^{\text{ter}}$ | 全局或局部地形摘要 | 是 | 否 |
| $s^{\text{oracle}}$ | $p^{*}$、$d_i^{*}$、$\bar d^{*}$、$\|\bar p-p^{*}\|$ | 是 | 否 |

critic 使用 oracle 信息的目的为改善价值估计与训练引导，不改变执行阶段的分散策略输入。

---

## 9. 动作空间

为降低当前训练难度，actor 不输出多锚点轨迹骨架，而输出低维局部子目标动作：

$$
a_i(t)=
\left[
\rho_i(t),\beta_i(t)
\right].
$$

其中：

$$
\rho_i(t)\in[0,\rho_{\max}],
$$

$$
\beta_i(t)\in[-\beta_{\max},\beta_{\max}].
$$

$\rho_i(t)$ 表示车体系下局部子目标距离，$\beta_i(t)$ 表示局部子目标方位角。对应的车体系局部子目标点为：

$$
p_{i,\text{sub}}^{b}(t)=
\begin{bmatrix}
\rho_i(t)\cos\beta_i(t)\\
\rho_i(t)\sin\beta_i(t)
\end{bmatrix}.
$$

若需要高程信息，则由局部地形函数补全；在高保真评估中再由 Isaac/PhysX 场景查询对应地形高度：

$$
z_{i,\text{sub}}=H(x_{i,\text{sub}},y_{i,\text{sub}}).
$$

单智能体动作维度为：

$$
d_a=2.
$$

多智能体动作张量形状为：

$$
[N,2].
$$

| 字段 | 记号 | 维度 | 坐标系 | 约束 | 含义 |
|---|---|---:|---|---|---|
| 局部子目标距离 | $\rho_i$ | 1 | 车体系 | $[0,\rho_{\max}]$ | 决定局部前进尺度 |
| 局部子目标方位角 | $\beta_i$ | 1 | 车体系 | $[-\beta_{\max},\beta_{\max}]$ | 决定局部运动方向 |

---

## 10. 轨迹生成器

轨迹生成器输入为低维局部子目标动作：

$$
G_{\text{traj}}:\ [\rho_i(t),\beta_i(t)] \mapsto \mathcal{T}_i(t).
$$

输出为带时间戳的局部参考轨迹：

$$
\mathcal{T}_i(t)=
\{(x_{i,k},y_{i,k},z_{i,k},\psi_{i,k},t_{i,k},v_{i,k})\}_{k=1}^{K}.
$$

轨迹生成流程如下：

1. 将 $[\rho_i,\beta_i]$ 转换为车体系局部子目标点；
2. 将局部子目标点转换到世界坐标系；
3. 查询地形高度或局部高程函数，补全 $z$；
4. 使用直线段、圆弧段或二阶 Bézier 曲线生成短局部路径；
5. 根据固定速度、限速规则和地形代价分配时间戳；
6. 输出固定长度 $K$ 的 time-stamped local trajectory。

当前阶段轨迹生成器采用确定性规则，不由 RL 学习速度剖面和轨迹曲率。

---

## 11. 简化控制执行层

由于月球车底层控制接口尚未确定，当前方案采用简化速度跟踪控制器。控制器根据局部参考轨迹生成车体速度命令：

$$
u_i(t)=
\left[
v_i^{\text{cmd}}(t),
\omega_i^{\text{cmd}}(t)
\right].
$$

其中，$v_i^{\text{cmd}}$ 为期望前向速度，$\omega_i^{\text{cmd}}$ 为期望偏航角速度。

Proxy 训练中，$[v_i^{\text{cmd}},\omega_i^{\text{cmd}}]$ 直接进入 2D/2.5D kinematic unicycle 状态更新：

$$
\psi_i(t+\Delta t)=\psi_i(t)+\omega_i^{\text{cmd}}(t)\Delta t,
$$

$$
p_i^{xy}(t+\Delta t)=p_i^{xy}(t)+
\begin{bmatrix}
\cos \psi_i(t+\Delta t)\\
\sin \psi_i(t+\Delta t)
\end{bmatrix}
v_i^{\text{cmd}}(t)\Delta t.
$$

在地形 proxy 开启时，线速度会按坡度、可通行性等地形特征进行缩放，并用地形高度函数更新 $z$。该状态更新不包含质量、惯量、轮地接触、打滑、悬挂或 PhysX contact。

高保真评估中，控制适配器根据轮式资产接口，将 $[v_i^{\text{cmd}},\omega_i^{\text{cmd}}]$ 转换为 Isaac/PhysX 可执行的控制目标。可选接口包括：

1. 左右轮速度目标；
2. 转向角 + 前向速度目标；
3. 关节速度目标；
4. 关节力矩目标；
5. 简化底盘控制目标。

当前技术方案不把 articulation 控制接口作为训练主路径的一部分；它属于高保真评估和后续真实 rover asset 迁移时需要冻结的适配层。

---

## 12. 集合成功判据

集合成功采用纯几何聚集判据。设：

$$
\bar p(t)=\frac{1}{N}\sum_{i=1}^{N}p_i(t),
$$

$$
D_{\max}(t)=\max_{i,j}\|p_i(t)-p_j(t)\|,
$$

$$
\sigma_p^2(t)=\frac{1}{N}\sum_{i=1}^{N}\|p_i(t)-\bar p(t)\|^2.
$$

若在连续 $n_{\text{hold}}$ 个判定时刻内同时满足：

$$
D_{\max}(t)\le \varepsilon_D,
$$

$$
\sigma_p^2(t)\le \varepsilon_\sigma,
$$

$$
\|v_i(t)\|\le \varepsilon_v,\quad i=1,\dots,N,
$$

则判定集合成功。

其中，$\varepsilon_D$ 为最大 pairwise 距离阈值，$\varepsilon_\sigma$ 为团队分散度阈值，$\varepsilon_v$ 为速度阈值，$n_{\text{hold}}$ 为连续保持步数。

---

## 13. 奖励函数

总奖励定义为：

$$
r_t
=
w_g r_{\text{gather}}(t)
+
w_o r_{\text{oracle}}(t)
+
w_e r_{\text{energy}}(t)
+
w_s r_{\text{safety}}(t)
+
w_m r_{\text{motion}}(t)
+
w_c r_{\text{consistency}}(t)
+
w_T r_{\text{terminal}}(t).
$$

### 13.1 自组织聚集奖励

$$
r_{\text{gather}}(t)
=
\alpha_1\big(D_{\max}(t-1)-D_{\max}(t)\big)
+
\alpha_2\big(\sigma_p^2(t-1)-\sigma_p^2(t)\big).
$$

该项衡量团队几何收缩趋势，不依赖显式集合点。

### 13.2 oracle 辅助奖励

定义平均最优点距离：

$$
\bar d^{*}(t)
=
\frac{1}{N}\sum_{i=1}^{N}\|p_i(t)-p^{*}(t)\|.
$$

oracle 辅助奖励固定采用平均距离下降量：

$$
r_{\text{oracle}}(t)
=
\alpha_3\big(\bar d^{*}(t-1)-\bar d^{*}(t)\big).
$$

该项仅用于训练塑形，不作为执行期输入。

### 13.3 能耗代理奖励

当前训练主路径采用代理项：

$$
r_{\text{energy}}(t)
=
-\alpha_4 L(t)
-\alpha_5 C_{\text{slope}}(t)
-\alpha_6 C_{\text{turn}}(t)
-\alpha_7 C_{\text{terrain}}(t).
$$

其中，$L(t)$ 为路径长度或位移代价，$C_{\text{slope}}$ 为坡度代价，$C_{\text{turn}}$ 为转向代价，$C_{\text{terrain}}$ 为高风险地形代价。

在具备关节力矩和关节速度后，可扩展为物理能耗近似：

$$
C_{\text{energy}}(t)
=
\sum_{\tau=t}^{t+\Delta t_p}
\sum_q
|\tau_q(\tau)\dot q(\tau)|\Delta t_c.
$$

该扩展不属于当前 proxy 训练主路径的必要条件。

### 13.4 安全惩罚

$$
r_{\text{safety}}(t)
=
-\alpha_8 C_{\text{obs}}(t)
-\alpha_9 C_{\text{agent}}(t)
-\alpha_{10} C_{\text{near}}(t).
$$

其中，$C_{\text{obs}}$ 为障碍碰撞惩罚，$C_{\text{agent}}$ 为车间碰撞惩罚，$C_{\text{near}}$ 为危险近距惩罚。

### 13.5 运动质量奖励

由于当前动作空间已降为 $[\rho,\beta]$，运动质量项简化为：

$$
r_{\text{motion}}(t)
=
-\alpha_{11}C_{\text{turn}}(t)
-\alpha_{12}C_{\text{subgoal}}(t).
$$

其中，$C_{\text{turn}}(t)$ 用于抑制过大局部转向，$C_{\text{subgoal}}(t)$ 用于抑制长期输出过小 $\rho_i$ 导致的停滞行为。

### 13.6 一致性奖励

$$
r_{\text{consistency}}(t)
=
-\alpha_{14}
\sum_{i=1}^{N}
\left\|
\begin{bmatrix}
\rho_i(t)\\
\beta_i(t)
\end{bmatrix}
-
\begin{bmatrix}
\rho_i(t-1)\\
\beta_i(t-1)
\end{bmatrix}
\right\|_2^2.
$$

### 13.7 终端奖励

$$
r_{\text{terminal}}(t)=
\begin{cases}
+R_{\text{succ}}, & \text{集合成功},\\
-R_{\text{fail}}, & \text{碰撞/越界/超时},\\
0, & \text{其他}.
\end{cases}
$$

---

## 14. 网络结构

### 14.1 actor 网络

actor 采用参数共享结构。每个智能体共享同一套策略网络参数。输入为局部观测 $o_i(t)$，输出为 2 维连续动作：

$$
a_i(t)=[\rho_i(t),\beta_i(t)].
$$

actor 由四个编码分支和一个共享 MLP 主干组成：

| 分支 | 输入 | 作用 |
|---|---|---|
| ego encoder | $o_i^{\text{ego}}$ | 编码自车状态 |
| neighbor encoder | $o_i^{\text{nbr}}$ | 编码邻居共享状态 |
| terrain encoder | $o_i^{\text{ter}}$ | 编码局部地形手工特征 |
| local-consensus encoder | $o_i^{\text{agg}}$ | 编码局部聚集态势 |

输出头为连续动作分布参数。动作采样后通过有界映射约束到：

$$
\rho_i\in[0,\rho_{\max}],
\quad
\beta_i\in[-\beta_{\max},\beta_{\max}].
$$

### 14.2 critic 网络

critic 采用 centralized value network。输入为全局状态 $s(t)$，输出团队共享状态价值：

$$
V_\phi(s(t)).
$$

critic 输入包含全部智能体真值状态、团队几何统计量、地形摘要和 oracle 辅助信息。当前阶段智能体数量固定为 4，因此 critic 可采用全局拼接输入和 MLP 主干结构。

---

## 15. SKRL-MAPPO 训练流程

训练流程包括：

1. proxy core 创建并行多智能体任务环境；
2. 每个环境包含 4 个 rover 智能体；
3. actor 根据局部观测输出 $[\rho,\beta]$；
4. proxy 环境内部将动作转换为参考轨迹和控制命令；
5. proxy unicycle 模型推进状态；
6. proxy 环境计算奖励、终止标志与下一观测；
7. SKRL 收集 rollout；
8. MAPPO 使用 centralized critic 计算优势并更新 actor/critic。

训练脚本可以通过 SKRL 的 `isaaclab-multi-agent` wrapper 接入多智能体接口，但该 wrapper 是数据接口适配层，不表示训练 loop 运行了 `SimulationApp`、PhysX 接触求解或真实 rover articulation。

---

## 16. 评估指标

评估指标分为 proxy 训练评估和 Isaac/PhysX 高保真闭环评估两类。

Proxy 任务指标包括集合成功率、平均完成时间、最大 pairwise 距离、团队分散度、平均邻距、碰撞率、超时率和 `dmax_reduction_ratio`。

oracle 最优性指标包括最终平均最优点距离 $\bar d^{*}(T)$、最终最大最优点距离、团队质心到 $p^{*}(T)$ 的距离和累计 oracle gap。

轨迹指标包括局部子目标变化幅度、轨迹长度、转向幅度、轨迹生成失败率和相邻规划周期动作差异。

Proxy strict gate 默认采用：

```text
dmax_reduction_ratio <= 0.2
success_rate >= 0.9
collision_rate <= 0.02
timeout_rate == 0
```

Isaac/PhysX 高保真闭环评估指标包括：

```text
rmse_cross_track_m
max_cross_track_m
path_completion_ratio
max_tilt_deg
collision / rollover / timeout diagnostics
```

高保真评估结果应表述为 checkpoint 或参考轨迹的 PhysX closed-loop validation，不应表述为训练 loop 已经在 Isaac Sim / PhysX 中完成。

---

## 17. 技术方案冻结结论

当前技术路线冻结为：

1. 采用 torch-vectorized proxy 环境作为 MAPPO/PPO 采样、reward 调试和 checkpoint selection 的训练主路径；
2. 采用 Isaac Sim / Isaac Lab / PhysX 作为 checkpoint 级高保真闭环评估平台；
3. 执行阶段不显式提供集合点；
4. 通信机制采用邻居状态共享；
5. actor 输出低维局部子目标动作 $[\rho,\beta]$；
6. 轨迹由确定性轨迹生成器生成；
7. proxy 训练中采用简化速度跟踪控制器和 2D/2.5D kinematic unicycle 状态更新；
8. critic 训练期接收全局状态与 oracle 辅助信息；
9. oracle 辅助奖励采用平均距离下降量；
10. 高保真评估中通过轮式资产控制适配器验证轨迹跟踪、姿态稳定性、碰撞和地形通过性；
11. 结果表述必须区分 proxy strict pass、PhysX closed-loop evaluation 和真实 Isaac 物理训练。

---

## 18. 技术边界

| 技术边界 | 当前处理方式 | 含义 |
|---|---|---|
| 训练动力学 | 2D/2.5D kinematic unicycle proxy | 不包含质量、惯量、悬挂、轮壤接触或 PhysX contact |
| 高保真评估 | Isaac/PhysX 轮式资产闭环跟踪 | 用于迁移 sanity check 和失败分析，不参与每次策略更新 |
| 当前轮式资产 | Jackal placeholder | 可验证控制链路，但不能代表最终 lunar rover asset |
| 地形输入 | 低维结构化特征 | 视觉、点云和 DEM patch 当前不进入 policy input |
| Oracle 信息 | critic / reward / metrics 可用，actor 不可见 | 保持 centralized training、decentralized execution 边界 |
| 动作接口 | $[\rho,\beta]$ 局部子目标 | 暂不把高维轨迹骨架作为 actor 直接输出 |

---
