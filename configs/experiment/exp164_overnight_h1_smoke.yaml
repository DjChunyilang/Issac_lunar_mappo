extends: exp164_overnight_h1_repaired_n1.yaml

experiment:
  name: exp164_overnight_h1_smoke
  num_envs: 256
  rollout_steps: 64
  eval_num_envs: 16
  eval_steps: 32
  checkpoint_interval: 0

bounded_curriculum:
  enabled: false
  stages: []

evaluation:
  proxy_eval:
    enabled: false
