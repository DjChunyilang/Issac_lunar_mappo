extends: exp125_decentralized_tiered_b0_pure_rl_relative_quintic.yaml

experiment:
  name: exp126_decentralized_b0_centered_terrain_credit

algorithm:
  training_semantics: exp126_decentralized_b0_centered_terrain_credit
  # Keep the team reward and centralized critic target unchanged. The shared
  # Actor alone receives a zero-sum temporal credit that distinguishes each
  # rover's selected quintic risk from its same-distance straight reference.
  actor_credit_assignment: terrain_relative_centered
  actor_credit_scale: 0.25
  actor_credit_trace_lambda: 0.95
