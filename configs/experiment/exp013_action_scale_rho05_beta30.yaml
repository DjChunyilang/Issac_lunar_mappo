experiment:
  name: exp013_action_scale_rho05_beta30
  seed: 7
  num_envs: 32
  device: cuda
  total_steps: 20000
  rollout_steps: 32
  log_dir: outputs/logs/exp013_action_scale_rho05_beta30
  checkpoint_dir: outputs/checkpoints
  checkpoint_name: exp013_action_scale_rho05_beta30.pt
  run_dir: outputs/runs
  save_checkpoint: true
  require_skrl_import: true

simulation:
  episode_length_s: 20.0
  physics_dt: 0.05
  control_decimation: 4

task:
  n_agents: 4

planner:
  rho_max: 0.5
  beta_max: 0.5235987756

algorithm:
  mode: pure_rl
  training_semantics: exp013_action_scale_rho05_beta30
  shared_actor: true
  centralized_critic: true
  shared_value: true
  gamma: 0.99
  learning_rate: 0.0005
